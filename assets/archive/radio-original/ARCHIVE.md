# Original HELLA_RADIO

This folder preserves the radio app that was served at `/radio` before the
2026-07-24 interface replacement. It is not part of the live public route.

The replacement app now lives in `client/public/radio`, and the existing
public URL remains `/radio`.
